# NGD v0.6.3 测试集群复测（2026-09-23）

测试环境：测试集群 `welkin-system` 中的 Platform Resource Controller 与 Algorithm。部署使用内部镜像标签 `v0.6.2`，镜像内容来自本地打包的 `v0.6.3`。Algorithm 使用 `sw-DC102.yaml` 对应的原始拓扑；当前有 3 台可解析拓扑的候选节点（paasmaster1/2/3）。

本次给 10 个 NGD 加独立运行后缀逐项创建、读取 NGD/NGG 状态，然后删除本轮对象。原有 `test-ngd-01-single-best`、`test-ngd-09-quota-too-small` 及原有 01 NGG 保留。

| 用例 | NGD / NGG | 选中节点或失败原因 |
|---|---|---|
| 01 单节点择优 | Fulfilled / Active | paasmaster1；101830m、457710Mi |
| 02 指定节点 | Fulfilled / Active | paasmaster2；71890m、429908Mi |
| 03 两节点资源 | Fulfilled / Active | paasmaster1、paasmaster3；189260m、902788Mi |
| 04 三节点资源 | Fulfilled / Active | paasmaster1、paasmaster3、paasmaster2；261150m、1332696Mi |
| 05 同拓扑域 | Fulfilled / Active | 3 个节点；Leaf、Spine、Border `requiredSame` 约束通过 |
| 06 指定物理 Leaf | Fulfilled / Active | 3 个节点；归入同一 Leaf Domain |
| 07 不匹配的选择器 | Failed / 无 NGG | `NODE_SELECTOR_NO_MATCH`：0/3 个拓扑节点匹配 |
| 08 资源不足 | Failed / 无 NGG | `INSUFFICIENT_RESOURCES`：要求 400000m、2097152Mi；上限 261150m、1332696Mi |
| 09 配额过小 | Failed / 无 NGG | `QUOTA_PREVENTS_MINIMUM`：3 个节点匹配，但 80000m、409600Mi 配额无法容纳满足最低资源的节点组 |
| 10 无效拓扑 | Failed / 无 NGG | `INVALID_TOPOLOGY_LABELS`：`NOT-EXIST-DC` 不在拓扑中 |

首轮运行时，原 05 用例的 `spine-switch: spine-01` 不在当前拓扑，正确返回 `TOPOLOGY_SWITCH_NOT_FOUND`。已把 [05-topology-same-leaf.yaml](./05-topology-same-leaf.yaml) 改为 `spine-switch: requiredSame`，完整重跑后 10 项均符合预期。成功用例均选择 `leaf-domain:pair-8e3df4c9cdcb`。

Algorithm 日志显示 14 项 Prometheus 指标刷新成功、覆盖 9 个 IP；分配阶段仍报告 `degraded=true, warningCount=1`，日志尚未列出告警正文。本次 NGD 功能测试通过，但不能据此认定降级告警已消除。

清理核对：本轮后缀 `r0923160638` 的 NGD/NGG 均已删除；集群仅剩上述原有的 2 个 NGD 和 1 个 NGG。
